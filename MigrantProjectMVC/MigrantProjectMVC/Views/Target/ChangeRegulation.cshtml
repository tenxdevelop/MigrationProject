<!DOCTYPE html>
<html>
<style>
    body {
        padding: 20px;
        background-color: #f8f9fa;
    }

    .form-container {
        max-width: 800px;
        margin: 0 auto;
        background-color: white;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 0 15px rgba(0,0,0,0.1);
    }

    .page-title {
        color: #0d6efd;
        margin-bottom: 30px;
        text-align: center;
    }

    .action-buttons {
        margin-top: 20px;
        display: flex;
        gap: 10px;
    }

    textarea {
        min-height: 300px;
    }
</style>
<head>
    <meta charset="UTF-8">
    <title>Target Viewer</title>
</head>
<body>
    <h1>Выберите Target</h1>
    <form id="profileForm" class="mt-4" method="post" asp-controller="Target" asp-action="ChangeRegulation">
        <select id="ChangeRegulationTargetSelect" class="form-select">
            <option value="">-- Выберите цель --</option>
        </select>
        <input name="targetName" id="targetNameInput" style="visibility: hidden" />
        <select id="regulationSelect" style="visibility: hidden" class="form-select">
            <option value="">-- Выберите условия --</option>
        </select>
        <input name="regulationName" id="ChangeRegulationTargetInput" style="visibility: hidden" value="sdawd"  class=""/>

        <select id="countrySelect" onchange="addToCountryForm()" style="visibility: hidden" class="form-select mt-5 mb-3" >
            <option value="">-- Выберите страны --</option>
            @{
                foreach (var item in ViewBag.Countries)
                {
                    <option value="@item">@item</option>
                }
            }
        </select>
        <div id="countryBox" style="visibility: hidden">
            <p>Имеющиеся документы: </p>
        </div>
        <div id="countryBox" class="">
            <input type="hidden" id="countryNames" name="countriesName" value="">
        </div>


        <select onchange="addToForm()" id="documentSelect" style="visibility: hidden" class="form-select mt-5 mb-3">
            <option value="">-- Выберите документы --</option>
            <option value="HighlyQualifiedDocument">Высоквалифицированный специалист</option>
            <option value="ResettlementDocument">Член программы переселения</option>
            <option value="ConsistOfMigrationProgramDocument">Состоял на миграционном учёте раннее</option>
        </select>
        <div id="documentBox" style="visibility: hidden">
            <p>Имеющиеся документы: </p>
        </div>
        <div id="documentBox">
            <input type="hidden" id="documentNames" name="useDocuments" value="">
        </div>
        <div class="d-flex flex-column mt-2">
            <div class="mb-4">
                <input id="termInput" name="term" style="visibility: hidden" class="form-control" />
            </div>
            <div class="d-flex flex-row">
                <button class="btn btn-primary me-3" type="submit" >Сохранить</button>
                <a type="button" class="btn btn-outline-info" href="../Hone/Index">Отмена</a>
            </div>
        </div>
    </form>


    <script>
           document.addEventListener("DOMContentLoaded", function () {
               loadTargetsChangeRegulation()
        });
    </script>
</body>
</html>
