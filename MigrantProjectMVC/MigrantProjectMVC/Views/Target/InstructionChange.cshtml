@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@{

}

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Target Viewer</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding: 20px;
            background-color: #f8f9fa;
        }

        .form-container {
            max-width: 800px;
            margin: 0 auto;
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
        }

        .page-title {
            color: #0d6efd;
            margin-bottom: 30px;
            text-align: center;
        }

        .action-buttons {
            margin-top: 20px;
            display: flex;
            gap: 10px;
        }

        textarea {
            min-height: 300px;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="form-container">
        <h1 class="page-title">Выберите Target</h1>

        <form id="profileForm" method="post" asp-controller="Target" asp-action="ChangeCondition">
            <div class="mb-3">
                <label for="targetSelect" class="form-label">Выберите цель:</label>
                <select class="form-select" id="targetSelect" aria-label="Выбор цели">
                    <option value="" selected>-- Выберите цель --</option>
                </select>
                <input name="targetName" id="targetNameInput" type="hidden" />
            </div>

            <div id="instructionBlock" class="mb-3" style="display: none;">
                <label for="instructionTextArea" class="form-label">Инструкция:</label>
                <textarea class="form-control" name="newInsctuction" id="instructionTextArea" rows="8"></textarea>
            </div>

            <div class="action-buttons">
                <button type="submit" class="btn btn-primary">Сохранить</button>
                <a href="../Home/Index" class="btn btn-outline-secondary">Отмена</a>
            </div>
        </form>
    </div>
</div>

<!-- Bootstrap 5 JS Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
           document.addEventListener("DOMContentLoaded", function () {
               loadTargetsInstructionPage()
        });
    </script>
</body>
</html>