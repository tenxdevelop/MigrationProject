@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Target Viewer</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            padding: 20px;
            background-color: #f8f9fa;
        }

        .form-container {
            max-width: 800px;
            margin: 0 auto;
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0,0,0,0.1);
        }

        .page-title {
            color: #0d6efd;
            margin-bottom: 30px;
            text-align: center;
        }

        .action-buttons {
            margin-top: 20px;
            display: flex;
            gap: 10px;
        }

        textarea {
            min-height: 300px;
        }
    </style>
</head>
<body>
    <form id="profileForm" class="mt-4" method="post" asp-controller="Target" asp-action="ChangeRegulation">
        <select id="countrySelect" onchange="addToCountryForm()"  class="form-select mt-5 mb-3">
            <option value="">-- Выберите страны --</option>
            @{
                foreach (var item in ViewBag.Countries)
                {
                    <option value="@item">@item</option>
                }
            }
        </select>
        <div id="countryBox" >
            <p>Имеющиеся документы: </p>
        </div>
        <div id="countryBox" class="">
            <input type="hidden" id="countryNames" name="countriesName" value="">
        </div>


        <select onchange="addToForm()" id="documentSelect"  class="form-select mt-5 mb-3">
            <option value="">-- Выберите документы --</option>
            <option value="HighlyQualifiedDocument">Высоквалифицированный специалист</option>
            <option value="ResettlementDocument">Член программы переселения</option>
            <option value="ConsistOfMigrationProgramDocument">Состоял на миграционном учёте раннее</option>
        </select>
        <div id="documentBox" >
            <p>Имеющиеся документы: </p>
        </div>
        <div id="documentBox">
            <input type="hidden" id="documentNames" name="useDocuments" value="">
        </div>
        <div class="d-flex flex-column mt-2">
            <div class="mb-4">
                <input id="termInput" name="term"  class="form-control" />
            </div>
            <div class="d-flex flex-row">
                <button class="btn btn-primary me-3" type="submit">Сохранить</button>
                <a type="button" class="btn btn-outline-info" href="../Home/Index">Отмена</a>
            </div>
        </div>
    </form>
</body>